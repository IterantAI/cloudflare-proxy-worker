name = "iterant-proxy"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[vars]
TARGET_HOST = "sites.iterant.ai"
CACHE_TTL = "3600"

# BRAND_ID should be set as a secret:
# wrangler secret put BRAND_ID
